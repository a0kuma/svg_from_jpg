# svg_from_jpg

Fast and dirty JPG-to-SVG conversion.

## Usage

```bash
python svg_from_jpg.py input.jpg output.svg
```

If `output.svg` is omitted, the script writes `<input-name>.svg` next to the input file.